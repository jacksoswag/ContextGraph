# graph-intelligence python dependencies, derived from the imports in 51 file(s).
# Pinned versions are what the code last ran against; unpinned ones were never
# installed on this machine, so no version is claimed.

numpy==2.5.0
pytest==9.1.1
requests==2.34.2
sentence-transformers==5.6.0
torch==2.12.1
trafilatura==2.1.0

# never installed here, version unknown
fastapi
hnswlib
pydantic
python-dotenv
spacy
sqlite_vec
uvicorn
